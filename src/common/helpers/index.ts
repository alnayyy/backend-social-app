import * as fh from './formatHelper';

const helpers = {
  formatDate: fh.formatDate,
  truncate: fh.truncate,
  formatNumber: fh.formatNumber,
  renderBadge: fh.renderBadge,
  imageUrl: fh.imageUrl,
};

export default helpers;
